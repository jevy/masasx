<% for notification_type in [:error, :notice, :warning] %>
  <% unless flash[notification_type].blank? %>
    <div class="alert-<%= notification_type %> alert">
      <a class="close" data-dismiss="alert">×</a>
      <%= content_tag :div, flash[notification_type] %>
    </div>
  <% end %>
<% end %>
