<% title 'Step 3 - Primary Contact Information' %>

<% content_for :sidebar do -%>
  <p>
  Please provide contact details for the person making this application. There
  will be a need to communicate with you during the verification process, so
  please ensure your contact information is complete and accurate. This
  contact information will be kept on file for administrative purposes, but
  does not have to be viewable by other users.
  </p>
<% end -%>

<ul class="nav nav-pills">
  <li>
  Step 1
  </li>
  <li>
  Step 2
  </li>
  <li class="active">
  Step 3
  </li>
  <li><a>Step 4</a></li>
</ul>

<%= simple_form_for @organization, url: next_step_path, method: :put do |f| %>

  <%= f.fields_for :primary_organization_administrator do |administrator| %>

    <%= administrator.fields_for :contact_info do |contact| %>

      <%= contact.input :name, label: 'Name' %>

      <%= contact.input :title, label: 'Title' %>

      <%= contact.input :language, label: 'Language', collection: Language.all %>

      <%= administrator.input :email, label: 'Office e-mail' %>

      <%= contact.input :mobile_email, label: 'Mobile e-mail' %>

      <%= contact.input :office_phone, label: 'Office Phone' %>

      <%= contact.input :mobile_phone, label: 'Mobile Phone' %>

    <% end %>

  <% end %>

 Information  <%= link_to 'Previous', previous_step_path(@organization), class: 'btn'  %>
  <%= f.button :submit, 'Next', class: 'btn-primary' %>

<% end %>
