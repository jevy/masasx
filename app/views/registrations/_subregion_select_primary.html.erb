<div id="state_wrapper">
  <div class="control-group state optional primary_contact_state">
    <label class="string optional control-label" for="organization_primary_organization_administrator_attributes_state">State/Prov</label>
    <div class="controls">
      <% primary_organization_administrator ||= :primary_organization_administrator %>
      <% country ||= params[:country] %>
      <% country = Carmen::Country.coded(country) %>

      <% if country.nil? %>
        <em>Please select a country above</em>
      <% elsif country.subregions? %>
        <%= subregion_select(primary_organization_administrator, :state, country, {}, {:name => 'organization[primary_organization_administrator_attributes][state]'} ) %>
      <% else %>
        <%= text_field(primary_organization_administrator, :state, :name => 'organization[primary_organization_administrator_attributes][state]') %>
      <% end %>
    </div>
  </div>
</div>
